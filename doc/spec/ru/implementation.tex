\subsection{Вопросы реализации}
В данном разделе будут перечислены проблемы, решение которых возлагается на реализацию
спецификации:
\begin{itemize}
    \item Типом элемента может быть любой тип определенный на реализуемой платформе (в том числе элементарный).
%    \item Набор элементарных типов языка определяются реализацией. Например, если
%данная спецификация реализуется для виртуальной Java машины (JVM) или для платформы .Net, то было 
%бы естественно оперировать теми типами, которые поддерживает соответсвующая платформа.

    \item Представление кортежа.
%	определяется реализацией. (Например, в Oqlite4j кортеж
%	представлен с помощью специального интерфейса javax.persistence.Tuple, который определен в
%	Criteria API 2.0).

    \item Способ задания сокращений (см.~\ref{cut_query}).

    \item Механизмы вызова и языки реализации функций.

\end{itemize}


\subsection{ЯЗ4J}
